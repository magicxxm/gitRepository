package com.mushiny.comm;

import com.mushiny.beans.Robot;

import java.util.Map;

/**
 * Created by Tank.li on 2017/10/25.
 */
public class ThreadTest {
    public static void main(String[] args) {
        /*TestThread thread1 = new TestThread("TestThread1");
        TestThread thread2 = new TestThread("TestThread2");
        Agv agv = new Agv();
        thread1.setAgv(agv);
        thread2.setAgv(agv);
        agv.setAvailable(true);
        agv.setStatus(Robot.IDLE);
        thread2.start();
        thread1.start();

        System.out.println("end");*/

        /*Map res = this.podRelease(sectionId,workStationId,podName);
        if(res == null || res.get("PodName").equals(podName)){
            try {
                Thread.sleep(2000L);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
            res = this.callNewPod(sectionId,workStationId);
        }*/
        //check

        //do others

    }
}
