/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.aricojf.util;

/**
 * jar文件读取管理
 *
 * @author aricochen
 */
public class JarFileManager {

   /* static Logger LOG = Logger.getLogger(JarFileManager.class);

    //从ｊar文件中读取文件
    public static URI readFileByName(String fileName) {
        try {
            return JarFileManager.class.getClassLoader().getResource(fileName).toURI();
        } catch (Exception e) {
            LOG.error(Global.LINE + "从jar文件中读取文件(fileName=" + fileName + ")信息出错:" + e.getMessage());
            return null;
        }
    }*/
}
